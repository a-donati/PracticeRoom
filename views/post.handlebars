  <header id="main-header" class="py-2 bg-dark text-white">
    <div class="container">
      <div class="row">
        <div class="col-md-6">
          <h1>
            Post {{id}}</h1>
        </div>
      </div>
    </div>
  </header>

<div class="d-flex flex-column">
  <h2 class="card-title mt-2">{{title}}</h2>
  <p>{{{content}}}</p>
  <p>{{description}}</p>
  <p>Created by {{post.user.displayName}} on {{format_date date_created}}</p>
</div>
<br>
{{!-- <div class="d-flex align-items-center"> --}}
{{> comment}}

{{!-- </div> --}}
{{!-- comments partial, will only render if comments exist --}}

<div class="d-flex justify-content-center">
  <form class="form new-comment-form">
    <div class="form-group d-flex flex-column">
      <label for="comment-content"> Add a comment: </label>
      <textarea class="form-input" id="comment-content" post_id={{id}} user_name={{user.displayName}}
        name="comment-content"></textarea>
    </div>
    <div class="form-group">
      <button type="submit" class="btn btn-primary mr-auto">Submit</button>
    </div>
  </form>
</div>

{{!-- <script src="/js/post.js"></script> --}}
<script src="/js/comment.js"></script>







